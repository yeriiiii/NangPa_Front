import { BrowserRouter, Route, Routes } from "react-router-dom";

const RootRoute = () => {
  return (
    <BrowserRouter>
      <Routes></Routes>
    </BrowserRouter>
  );
};

export default RootRoute;
